# Тест видимости ветра / Wind Visibility Test

## Что было изменено / What was Changed

Увеличены коэффициенты, чтобы эффект ветра был более видимым:

### Коэффициенты ветра / Wind Coefficients

| Параметр | Было | Стало | Увеличение |
|----------|------|-------|-----------|
| Горизонтальное ускорение | 0.5 | 3.0 | **6x** |
| Вертикальный лифт | 0.3 | 2.0 | **6.7x** |

## Как протестировать / How to Test

### Быстрый способ - плейграунд / Quick Way - Playground

```bash
pnpm run playground
```

Затем откройте http://localhost:5173/playground/ и:
1. Включите ветер в левой панели (`Wind Enabled: ON`)
2. Установите `Wind Strength: 0.8 или выше`
3. Посмотрите как снежинки дрейфуют в стороне с видимым эффектом

### Через браузер / Through Browser

1. Установите расширение в браузере
2. Откройте консоль (F12)
3. Включите ветер в попапе расширения
4. Установите силу ветра (Wind Strength) на максимум
5. В консоли вы должны увидеть логи типа:

```
🌬️ Wind is blowing with turbulence: {
  direction: "left",
  strength: 0.5,
  force: -0.32,
  turbulence: -0.63
}
```

## Ожидаемое поведение / Expected Behavior

### С силой ветра 0.5+ / With Wind Strength 0.5+

- ✅ Маленькие снежинки **явно дрейфуют** в стороне (видимо сносятся ветром)
- ✅ Большие снежинки **немного наклоняются** к ветру (более инертны)
- ✅ Видны **волнообразные порывы** ветра (меняется скорость и направление)
- ✅ При сильных порывах маленькие снежинки **могут немного подняться вверх** (лифт)

### После включения / After Enabling

После включения ветра в настройках, снежинки должны начать дрейфовать. Если они падают прямо вниз - значит проблема с коэффициентами.

## Физика / Physics

Ветер воздействует на каждую снежинку в зависимости от:

1. **Размера снежинки**: `crossSection = (size / 20)²`
   - Размер 10 → crossSection = 0.25 (слабо)
   - Размер 20 → crossSection = 1.0 (нормально)
   - Размер 40 → crossSection = 4.0 (сильно)

2. **Силы ветра**: от -1 до +1 в каждый момент времени

3. **Коэффициента усиления**:
   - Горизонтальный ветер: `windAccel × 3 × delta`
   - Вертикальный лифт: `liftAccel × 2 × delta`

## Если ветер не виден / If Wind is Not Visible

Проверьте:
1. ✓ `windEnabled: true` в конфигурации
2. ✓ `windStrength: >= 0.5` (чем выше, тем сильнее)
3. ✓ `windGustFrequency: > 0` (частота порывов)
4. ✓ Консоль браузера показывает логи ветра
5. ✓ Размер снежинок достаточный (10+ пикселей)

## История изменений / Changelog

### v1.0 - Усиление видимости ветра / Wind Visibility Enhancement
- Увеличены коэффициенты с 0.5 → 3.0 (горизонталь)
- Увеличены коэффициенты с 0.3 → 2.0 (вертикаль)
- Эффект ветра теперь явно виден при включении
